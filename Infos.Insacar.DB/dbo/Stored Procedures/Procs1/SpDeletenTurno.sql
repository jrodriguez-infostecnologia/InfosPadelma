create PROCEDURE [dbo].[SpDeletenTurno] @empresa int,@codigo varchar(50),@Retorno int output  AS begin tran nTurno delete nTurno where codigo = @codigo and empresa = @empresa if (@@error = 0 ) begin set @Retorno = 0 commit tran nTurno end else begin set @Retorno = 1 rollback tran nTurno end